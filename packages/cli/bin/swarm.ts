#!/usr/bin/env bun
// swarm CLI entry point. Phase 2+ will wire subcommands via cac.

import { SWARM_CLI_VERSION } from "../src/index.ts";

console.log(`swarm ${SWARM_CLI_VERSION} — scaffold only, no commands yet. See docs/PLAN.md.`);
