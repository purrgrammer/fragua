// Coverage for the per-parent descendant SSE route was retired
// together with sub-runs. This file is intentionally empty until
// `storeRunsRoutes` grows a new test surface.

import { describe, test } from "bun:test";

describe("storeRunsRoutes", () => {
  test.skip("placeholder — descendant SSE coverage retired with sub-runs", () => {});
});
