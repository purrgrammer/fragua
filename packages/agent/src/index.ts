// @swarm/agent — CodergenBackend implementations over pi-agent-core.
// Adds: checkpoint granularity, loop detection, per-tool truncation,
// permission modes, subagent helper, event sink bridge.

export const SWARM_AGENT_VERSION = "0.0.0";
