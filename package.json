{
  "name": "swarm",
  "version": "0.0.0",
  "private": true,
  "description": "Universal AI agent orchestrator",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "bun run --filter='*' typecheck",
    "lint": "biome check .",
    "format": "biome format --write .",
    "test": "bun test ./packages",
    "ci": "bun run lint && bun run typecheck && bun test ./packages"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "@types/bun": "1.2.17",
    "typescript": "5.7.3"
  },
  "engines": {
    "bun": ">=1.2.0",
    "node": ">=20.0.0"
  }
}
